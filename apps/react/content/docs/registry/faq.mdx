---
title: FAQ
description: Clarify how the React registry relates to packages, styles, and the Vue site.
---

## Is the React registry separate from Vue?

Yes. React and Vue each publish their own registry root and item JSON files.

## Do I need the package to use the registry?

No. Registry items are copy-paste distributions. The package is optional.

## Why are styles a separate item?

Because consumers may want the design tokens and global SCSS before adopting any UI item.
