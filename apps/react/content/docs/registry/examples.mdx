---
title: Examples
description: Example registry compositions built from the current React item set.
---

The first examples focus on forms and settings surfaces because they exercise nearly every primitive in the current registry.

- `contact-form` uses card, input, textarea, label, and button.
- `newsletter-card` uses card, input, and button.
- `profile-settings` uses card, label, input, and button.
